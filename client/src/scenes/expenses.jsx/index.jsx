import { Box } from "@mui/material";
import React from "react";
import Form from "./Form";

const Expenses = () => {
    return (
        <Box>
            <Form />
        </Box>
    );
};

export default Expenses;
